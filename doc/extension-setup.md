# Extension setup via neon

you can use all parameters from [src/ImageSlaveExtension.php#L15](src/ImageSlaveExtension.php#L15) in your config.neon like this:

    imageslave:
        width: 300
        height: 300